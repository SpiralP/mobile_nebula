name: Merge upstream

on:
  schedule:
    - cron: "0 19 * * 1" # monday at noon
  workflow_dispatch:

concurrency:
  group: merge-upstream-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  pull-requests: write

jobs:
  merge_upstream:
    uses: SpiralP/github-reusable-workflows/.github/workflows/merge-upstream.yml@8db61cd94fd11042022ca656443eba2ed38db4a1 # main
    with:
      url: https://github.com/DefinedNet/mobile_nebula.git
